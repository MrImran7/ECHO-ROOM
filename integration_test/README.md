Device integration execution is pending a Flutter-capable host and emulator/device.
The authored widget flow in test/app_test.dart runs the actual Flame GameWidget
with an isolated repository. Controller tests cover fresh-container restoration.
The manual device checklist and build commands are in the root README.
